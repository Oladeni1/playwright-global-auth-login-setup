{
  "name": "login-auth-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "runHeadlessTest": "npx playwright test",
    "runHeadedTest": "npx playwright test --headed",
    "runTestWithAllureReport": "npx playwright test --reporter=line,allure-playwright && allure generate allure-results -o allure-report --clean && allure open allure-report",
    "runTestWithShowReport": "npx playwright test & npx playwright show-report",
    "runDebugTest": "npx playwright test --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.38.0",
    "allure-playwright": "^2.8.1"
  }
}
